from setuptools import setup, find_packages

setup(
    name='math-package',
    version='0.1',
    packages=find_packages(),
    url='',
    description='A toy project to do math',
    install_requires=[
    ],
    python_requires='>=3.11',
)
